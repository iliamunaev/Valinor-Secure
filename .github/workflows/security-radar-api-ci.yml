name: Security Radar API CI/CD Pipeline

on:
  push:
    branches:
      - main
      - master
      - develop
      - 'feature/**'
    paths:
      - 'security-radar-api/**'
      - '.github/workflows/security-radar-api-ci.yml'
  pull_request:
    branches:
      - main
      - master
      - develop
    paths:
      - 'security-radar-api/**'
      - '.github/workflows/security-radar-api-ci.yml'

jobs:
  lint:
    name: Lint and Type Check
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: security-radar-api
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install tox

      - name: Run linting
        run: tox -e lint

      - name: Run type checking
        run: tox -e type

  test:
    name: Test Python ${{ matrix.python-version }}
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: security-radar-api
    strategy:
      fail-fast: false
      matrix:
        python-version: ['3.10', '3.11', '3.12']

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install tox tox-gh-actions

      - name: Cache tox environments
        uses: actions/cache@v4
        with:
          path: security-radar-api/.tox
          key: tox-${{ runner.os }}-${{ matrix.python-version }}-${{ hashFiles('security-radar-api/requirements*.txt', 'security-radar-api/tox.ini') }}
          restore-keys: |
            tox-${{ runner.os }}-${{ matrix.python-version }}-
            tox-${{ runner.os }}-

      - name: Run tests with tox
        run: |
          case "${{ matrix.python-version }}" in
            "3.10") tox -e py310 ;;
            "3.11") tox -e py311 ;;
            "3.12") tox -e py312 ;;
          esac

  coverage:
    name: Coverage Report
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: security-radar-api
    needs: test
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install tox

      - name: Run coverage
        run: tox -e coverage

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v4
        with:
          file: ./security-radar-api/coverage.xml
          flags: unittests
          name: codecov-umbrella
          fail_ci_if_error: false
        env:
          CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}

      - name: Upload coverage reports as artifact
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: |
            security-radar-api/coverage.xml
            security-radar-api/htmlcov/
          retention-days: 30

  docs:
    name: Documentation Check
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: security-radar-api
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install tox

      - name: Check documentation
        run: tox -e docs

  all-checks-pass:
    name: All Checks Passed
    runs-on: ubuntu-latest
    needs: [lint, test, coverage, docs]
    if: always()
    steps:
      - name: Check if all jobs passed
        run: |
          if [[ "${{ needs.lint.result }}" != "success" ]] || \
             [[ "${{ needs.test.result }}" != "success" ]] || \
             [[ "${{ needs.coverage.result }}" != "success" ]] || \
             [[ "${{ needs.docs.result }}" != "success" ]]; then
            echo "One or more jobs failed"
            exit 1
          fi
          echo "All checks passed successfully!"
